{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/evento/(.*)", "destination": "/api/evento-preview?e=$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
