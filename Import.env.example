# Variáveis para a página de Eventos (Google Calendar)
#
# 1. Copie este arquivo:  cp Import.env.example Import.env
# 2. Abra Import.env e troque "sua_chave_api_aqui" e "primary" pelos seus valores.
# 3. Na Vercel: Settings > Environment Variables > adicione cada linha (nome = à esquerda do =, valor = à direita).
#    Ou cole o conteúdo de Import.env no editor em massa da Vercel, se disponível.
# 4. Para rodar local: copie o conteúdo de Import.env para .env (ou renomeie Import.env para .env).
#
# Não commite Import.env com valores reais (ele está no .gitignore).

VITE_GOOGLE_CALENDAR_API_KEY=sua_chave_api_aqui
VITE_GOOGLE_CALENDAR_ID=primary
